package com.example

import kotlin.String

interface GetAnimal {
  interface QueryData {
    val friend: _.AnimalFriend?

    interface AnimalFriend {
      val asCatFriend: _.CatFriend

      val asLionFriend: _.LionFriend

      interface CatFriend {
        val species: String
      }

      interface LionFriend {
        val species: String
      }
    }
  }
}
