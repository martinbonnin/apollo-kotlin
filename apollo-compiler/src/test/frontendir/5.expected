package com.example

import kotlin.String

interface GetAnimal {
  interface Query {
    val randomAnimal: com.example.Animal?

    interface Animal

    interface AnimalCatOrAnimalLion {
      val species: String
    }
  }
}
