package com.example

import kotlin.String

interface GetAnimal {
  interface QueryData {
    val random: _.AnyRandom?

    interface AnyRandom {
      val asPetRandom: _.PetRandom

      val asCatRandom: _.CatRandom

      interface PetRandom {
        val name: String

        val asCatRandom: _.CatRandom

        val asTurtleRandom: _.TurtleRandom

        interface CatRandom {
          val name2: String
        }

        interface TurtleRandom {
          val shell: String
        }
      }

      interface CatRandom {
        val name1: String

        val habitat: _.HabitatHabitat

        val asPetRandom: _.PetRandom

        interface HabitatHabitat {
          val description: String?
        }

        interface PetRandom {
          val name: String
        }
      }
    }
  }
}
