package com.example

import kotlin.String

interface GetAnimal {
  interface QueryData {
    val friend: _.AnimalFriend?

    interface AnimalFriend {
      val asCatFriend: _.CatFriend

      interface CatFriend {
        val name1: String?
      }
    }
  }
}
