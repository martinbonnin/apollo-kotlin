# Animal and AnimalCat have the same shape (only species) and should merge
query GetAnimal($condition: Boolean!) {
  randomAnimal {
    species
    ...catFragment @include(if: $condition)
  }
}

fragment catFragment on Cat {
  name
  habitat {
    temperature
  }
}