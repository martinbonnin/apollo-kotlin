package com.example

import kotlin.Float
import kotlin.String

interface GetAnimal {
  interface Query {
    val randomAnimal: com.example.Animal?

    interface AnimalCat {
      val species: String
    }

    interface AnimalCatCondition {
      val species: String

      val name: String

      val habitat: com.example.Habitat

      interface Habitat {
        val temperature: Float?
      }
    }

    interface Animal {
      val species: String
    }

    interface AnimalCondition {
      val species: String
    }
  }
}

interface CatFragment {
  interface Cat {
    val name: String

    val habitat: com.example.Habitat

    interface Habitat {
      val temperature: Float?
    }
  }
}
