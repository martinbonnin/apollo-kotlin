package com.example

import kotlin.Float
import kotlin.String

interface GetAnimal {
  interface Query {
    val randomAnimal: com.example.Animal?

    interface AnimalCatCondition : CatFragment {
      val species: String

      val name: String

      val habitat: com.example.Habitat

      interface Habitat {
        val temperature: Float?
      }
    }

    interface Animal {
      val species: String
    }
  }
}

interface CatFragment {
  interface Cat {
    val name: String

    val habitat: com.example.Habitat

    interface Habitat {
      val temperature: Float?
    }
  }
}
