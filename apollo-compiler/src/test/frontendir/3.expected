package com.example

import _.CatFragmentFragment
import kotlin.String

interface GetAnimal {
  interface QueryData {
    val friend: _.AnimalFriend?

    interface AnimalFriend {
      val species: String

      val asCatFragmentFragment: CatFragmentFragment
    }
  }
}
