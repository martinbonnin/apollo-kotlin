package com.example

import kotlin.String

interface GetAnimal {
  interface Query {
    val randomAnimal: com.example.Animal?

    interface AnimalCat {
      val species: String
    }

    interface Animal {
      val species: String?
    }
  }
}
